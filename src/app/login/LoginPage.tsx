import LoadingPage from "@/lib/loading/loadingPage";
import { FC } from "react";

const LoginPage: FC = () => {
  return (
    <div>
      <LoadingPage />
    </div>
  );
};

export default LoginPage;
